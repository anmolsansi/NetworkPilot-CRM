# v0.0.1–v0.0.10 Completion Report

The audit findings for the first ten versioned expansions are remediated on the
`codex/complete-v0.0.1-v0.0.10` branch.

## Delivered

- Atomic workspace-scoped bulk actions with legacy and normalized tag/stage contracts.
- Allowlisted, user-scoped saved views with unique names.
- Null-safe duplicate detection and allowlisted, audited merging.
- Durable asynchronous CSV jobs, 40-row checkpoints, stale-job recovery, three-attempt retry,
  persistent history, and downloadable error CSVs.
- Coloured tag management, filtering, bulk assignment, and dashboard counts.
- Notification centre, timezone-aware digest scheduling, email preferences, overdue alerts, and
  configurable quiet hours.
- Favourite stars and notes in the table and Chrome extension.
- Restored the extension Tailwind build pipeline so its utility-class UI is included in the bundle.
- Deleted-only Trash, guarded restore, unarchive, and immediate delete undo.
- Editable/reorderable coloured pipeline stages with configurable transition rules.
- Stable UUID-keyed custom values with server type validation and dropdown fields.
- Corrected extension/calendar route prefixes and restored accidentally dropped production indexes.

## Automated evidence

- Backend: Ruff clean and 83 tests passed, including a 41-row CSV job crossing the 40-row
  checkpoint plus focused v0.0.1–v0.0.10 tenant-boundary coverage.
- Frontend: ESLint clean, 26 tests passed, and the TypeScript/Vite production build passed.
- Extension: 12 tests passed and the TypeScript/Vite production build passed.
- API: 44 OpenAPI paths checked; required routes are present and no doubled calendar/extension
  prefixes remain.
- Database: Alembic has one head and generated the complete 530-line PostgreSQL upgrade chain.
- Dependencies: full npm audits, including development tooling, reported zero vulnerabilities for
  frontend and extension after upgrading Vite/Vitest.

## Release boundary

No version tags are created by this report. Apply Alembic migrations and record authenticated
staging browser QA before tagging releases.
