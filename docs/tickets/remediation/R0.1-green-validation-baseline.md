# TICKET R0.1: Restore the validation baseline

## Goal
Make backend integration tests and frontend lint/tests executable before feature remediation.

## Why This Exists
The release gates cannot prove any version while PostgreSQL JSONB prevents SQLite test setup, frontend mocks omit newly consumed APIs, and ESLint has no configuration.

## User-Facing Behavior
None (internal validation infrastructure).

## Scope
- Compile PostgreSQL JSONB as JSON in SQLite integration tests.
- Add missing API mocks used by People and Person Create tests.
- Add the repository ESLint configuration consumed by the frontend lint script.

## Out of Scope
- Feature behavior changes.
- Production database migrations.

## Files to Inspect First
- `backend/app/tests/integration/conftest.py` — SQLite type adapters.
- `frontend/src/pages/PersonCreatePage.test.tsx` — HTTP client mock.
- `frontend/src/pages/WorkspaceRequiredPages.test.tsx` — People dependencies.
- `frontend/package.json` — lint command and plugins.

## Files to Create or Edit
- `backend/app/tests/integration/conftest.py`
- `frontend/src/pages/PersonCreatePage.test.tsx`
- `frontend/src/pages/WorkspaceRequiredPages.test.tsx`
- `frontend/.eslintrc.cjs`

## Data Model
None.

## API Contract
None.

## UI States
None.

## Validation Rules
The test database must preserve JSON object/list behavior when PostgreSQL JSONB columns are compiled by SQLite.

## Error Handling
Missing frontend calls remain strict mock failures; every consumed API is explicitly mocked.

## Auth & Permissions
Existing mocked Supabase authentication remains unchanged.

## Dependencies
Current `origin/main` at `4925943`.

## Integration Contract
`pytest`, `npm test -- --run`, and `npm run lint` become trustworthy release gates.

## Implementation Steps
1. Add a SQLite JSONB compiler/test metadata adapter.
2. Add minimal API mocks returning deterministic empty data.
3. Configure ESLint for React, TypeScript parser behavior, hooks, and refresh rules.
4. Run focused and full gates.

## Test Cases
1. Backend integration fixture creates all metadata tables.
2. Person Create tests render with tag loading mocked.
3. People tests render with stages and saved views mocked.
4. Frontend lint executes using the checked-in configuration.

## Manual QA
1. Run all commands and confirm failures represent application behavior rather than missing configuration.

## Acceptance Criteria
- [ ] Backend tests pass test-database setup.
- [ ] Existing frontend tests pass.
- [ ] Frontend lint starts and reports code findings normally.

## Definition of Done
Apply `product_quality_bar.md` and `testing_rules.md`.

## Do Not Change
- Production models or migrations.
- API request/response shapes.
